/**
 * Solves part one of the puzzle for the given input.
 *
 * @param _input The puzzle input.
 * @returns The part one answer.
 */
export function solvePartOne(_input: string): number {
  return 0;
}

/**
 * Solves part two of the puzzle for the given input.
 *
 * @param _input The puzzle input.
 * @returns The part two answer.
 */
export function solvePartTwo(_input: string): number {
  return 0;
}
