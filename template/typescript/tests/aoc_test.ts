import { assertEquals } from "@std/assert";

import { solvePartOne, solvePartTwo } from "../src/aoc.ts";

Deno.test("part one solves the placeholder input", () => {
  assertEquals(solvePartOne(""), 0);
});

Deno.test("part two solves the placeholder input", () => {
  assertEquals(solvePartTwo(""), 0);
});
