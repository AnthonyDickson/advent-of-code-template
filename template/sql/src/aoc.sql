-- The two solutions. `main.sql` hands them the puzzle input, and the tests in
-- `tests/test_aoc.sql` hand them the examples from the puzzle statement.
--
-- Both are DuckDB macros, so `solve_part_one(input)` and `solve_part_two(input)`
-- take the whole puzzle input as one string and return a number.

-- Solves part one of the puzzle for `input`.
CREATE OR REPLACE MACRO solve_part_one(input) AS 0;

-- Solves part two of the puzzle for `input`.
CREATE OR REPLACE MACRO solve_part_two(input) AS 0;
