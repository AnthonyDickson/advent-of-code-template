// The two solutions. `main.typ` hands them the puzzle input, and the tests in
// `tests/test_aoc.typ` hand them the examples from the puzzle statement.

/// Solves part one of the puzzle for `input`.
#let solve-part-one(input) = 0

/// Solves part two of the puzzle for `input`.
#let solve-part-two(input) = 0
